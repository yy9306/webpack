"main";/*{"e":{"default":1},"m":{"postcss":["default"],"postcss-value-parser":["default"]},"s":[],"w":null}*/let postcss;_280‍.w('postcss',[["default",function(v){postcss=v}]]);let valueParser;_280‍.w('postcss-value-parser',[["default",function(v){valueParser=v}]]);


const fontFamilySystemUIList = [
  'system-ui',
  '-apple-system',
  'BlinkMacSystemFont',
  'Segoe UI',
  'Roboto',
  'Oxygen',
  'Ubuntu',
  'Cantarell',
  'Fira Sans',
  'Droid Sans',
  'Helvetica Neue'
].join(', ');

const transformFontFamilySystemUI = node => {
  if (node.type === 'word' && node.value === 'system-ui') {
    node.value = fontFamilySystemUIList
  }
};

const transform = () => (decl) => {
  if (decl.type === 'decl') {
    if (decl.prop === 'font-family' || decl.prop === 'font') {
      const tree = valueParser(decl.value)
      tree.walk(transformFontFamilySystemUI)
      decl.value = tree.toString()
    }
  }
}

_280‍.d(postcss.plugin('postcss-font-family-system-ui', () => {
  return (root) => {
    root.walk(transform())
  }
}));
